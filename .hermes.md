# Mnemonic

AI Agent 持久化记忆存储中间件。独立部署，多端共享。

## 一句话

给 Hermes / OpenClaw 等 AI Agent 提供统一的长期记忆存储服务——事实提取、向量检索、冲突消解、遗忘管理。

## 技术栈

FastAPI + SQLAlchemy 2.0 (async) + PostgreSQL 16 + pgvector + Docker Compose

## 项目结构

```
monic/
├── mnemonic/
│   ├── api.py          # FastAPI端点 (8个)
│   ├── store.py        # CRUD + 向量搜索
│   ├── services.py     # Embedding + LLM提取
│   ├── models.py       # ORM模型
│   ├── schemas.py      # Pydantic模型
│   ├── config.py       # YAML配置加载
│   ├── database.py     # DB引擎
│   └── provider.py     # Hermes MemoryProvider适配器
├── tests/              # pytest
├── config.yaml         # 运行时配置
├── init.sql            # PG Schema
├── docker-compose.yml  # PG 16 + pgvector
├── phase.md            # 开发进度与计划 ← 详细看这里
└── .hermes.md          # 本文件
```

## 运行

```bash
# 1. 启动PG
docker compose up -d

# 2. 启动API
MNEMONIC_DB_PASSWORD=mnemonic \
MNEMONIC_LLM_API_KEY=<key> \
.venv/bin/uvicorn mnemonic.api:app --host 0.0.0.0 --port 8010
```

端口: API `8010`, PG `5434`

## API速查

命名空间通过Header传递: `X-Namespace: client_id:user_id:agent_id[:session_id]`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/memories` | 创建 |
| GET | `/memories` | 列表 |
| GET | `/memories/{id}` | 单条 |
| PATCH | `/memories/{id}` | 更新 |
| DELETE | `/memories/{id}` | 软删除 |
| POST | `/memories/search` | 向量搜索 |
| POST | `/memories/extract` | LLM提取+存储 |

## 当前进度

**Phase 1 ✅ 完成** — CRUD + pgvector + LLM提取 + 冲突检测 + Hermes适配器骨架

**阻塞项:** Embedding方案未定(当前hash-based伪向量), Hermes兼容路线待选

→ **完整进度、计划、决策记录见 [`phase.md`](phase.md)**
